# random-password-generator
it is a password generator it takes the length of password as input and creates a random password with mixed characters including numbers and special symbols and saves them in a doc file
